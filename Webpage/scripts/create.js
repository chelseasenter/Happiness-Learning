var url = "http://127.0.0.1:5000/";

querlUrl = url + "insert the correct query"

var allowance = d3.select("#allowancebox").attr("value")
d3.select(#plusSign).on("click");

updateScore {
    firstbox = d3.select(#firstbox).
}

class CategoryLevel{
    constructor(category){
        this.category = category;
        this.score = 0;
    }

    addition(amount){
        this.score += amount;
        allowance -= allowance;
    }

    subtraction(amount){
        this.score -= 1;
        allowance += allowance
    }
}

